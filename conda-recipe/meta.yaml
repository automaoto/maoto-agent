package:
  name: maoto-agent
  version: 1.0.1

source:
  path: ..

build:
  noarch: python
  script: |
    pip install .
    pip install graphqlclient

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8

about:
  license_file: LICENSE
  summary: "Maoto Agent"
  description: "Maoto Agent"

extra:
  keywords:
    - Maoto
    - Agent
